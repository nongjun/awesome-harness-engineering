---
description: 从项目实践中提炼的架构设计和工程经验。涉及日志、计费、多模块前端、AI大模型接入、模型注册、macOS后台保活、事件驱动、asyncio线程池、事件循环阻塞、工具参数设计、容器部署连锁故障等场景时参考
alwaysApply: false
---

# 架构经验

## 日志 vs 计费 vs 业务数据——三类数据不能混

| 类型 | 存储 | 保留 | 用途 |
|------|------|------|------|
| 运维日志（报错/审计） | 日志平台 | 短期 | 排查问题 |
| 计费数据（用量统计） | 数据库聚合表 | 永久 | 向客户收费 |
| 业务记录（执行流水/审核） | 数据库业务表 | 跟随业务 | 业务流程展示 |

判断标准：有外键关联 / 按业务 ID 精确查询的是业务数据，留数据库；按时间范围模糊搜索的是日志，走日志平台；需要长期留存做报表的是计费数据，走聚合表。

## 不要创建与已有模块同名的包目录

`app/config.py` 已存在时，创建 `app/config/__init__.py` 会让 Python 优先导入目录包，导致原模块中的类全部找不到。新增配置文件放其他目录，避免命名冲突。

## 侧边栏菜单滚动——正确的 CSS 模式

错误做法：给 `.aside` 设 `overflow-y: auto`（整个侧边栏含 logo 一起滚，体验差）。

正确做法：

```css
.aside {
  display: flex;
  flex-direction: column;
  overflow: hidden;          /* 外层不滚 */
}
.aside .logo {
  flex-shrink: 0;            /* logo 固定 */
}
.aside .menu {
  flex: 1;                   /* 菜单占满 */
  overflow-y: auto;          /* 菜单自己滚 */
}
```

## 多模块前端改造——必须检查的清单

1. 路由路径：各模块 `createWebHistory('/base')` 有 base path，跳转链接必须包含 base
2. 菜单位置：新菜单项要放在用户容易找到的分组内，不要塞在最底部被截断
3. 前后端同步部署：前端 404 通常是后端没重建，不是前端问题
4. 共享组件 import 路径：共享组件用相对路径而非别名路径，避免跨模块引用失效

## 代码工厂模式——一行接入

写多模块共用的功能时，用路由工厂 + 组件 props 实现"写一次，各模块一行接入"：

- 后端：`create_xxx_router(module_code="模块名", db_getter=get_db)` → 返回 APIRouter
- 前端：`<XxxPanel :apiBase="/api/module/xxx" />` → 共享组件

## AI 大模型接入——集中注册制

所有 AI 模型的使用必须通过集中的模型注册与绑定机制，禁止在业务代码中硬编码模型名/API Key/Base URL。

流程：
1. 在管理后台注册模型，填写 name、base_url、api_key、model、capabilities
2. 为具体功能点选择首选/次选/兜底模型（3 槽位降级链）
3. 业务服务通过统一的 AIService 自动获取绑定配置，按槽位降级

禁止：
- 在代码中写死 `base_url="https://xxx"` 或 `model="xxx"` 绕过注册中心
- 通过环境变量覆盖模型选择（环境变量仅用于基础设施配置）
- 直接操作模型配置表（应通过管理 API 或界面操作）

## macOS 后台保活——三层防护

macOS 会通过 App Nap、自动终止等机制抑制后台应用。需要主动保活的场景（如 WebSocket 长连接）必须三层同时防护：

| 层级 | 手段 | 作用 |
|------|------|------|
| 进程级 | `ProcessInfo.beginActivity(.userInitiatedAllowingIdleSystemSleep)` | 禁用 App Nap，保持定时器和网络 |
| 系统级 | `Info.plist` 设 `NSSupportsAutomaticTermination = false` + `NSSupportsSuddenTermination = false` | 阻止系统自动杀进程 |
| 推送级 | APNs 静默推送（`content-available: 1`） | 进程被杀后的最终兜底唤醒 |

第一层在连接建立时启用、断开时释放；第二层是静态配置；第三层需要 Apple Developer Program。

## 事件驱动 + 轮询兜底——防卡死模式

纯事件驱动的状态机（如 `NSWorkspace.didLaunchApplicationNotification`）在以下场景会丢失事件：
- 进程被 App Nap 休眠期间事件触发
- 事件发送时观察者尚未就绪
- 系统负载高时通知被合并或丢弃

**模式**：关键状态转换除了监听事件外，必须加定时轮询作为兜底（典型 5-10 秒间隔），并在 App 回到前台时主动检测一次。轮询在状态恢复后立即停止。

## fire-and-forget 异步任务必须自带异常捕获

`asyncio.ensure_future()` 或 `Task { }` 启动的后台任务，如果内部抛异常：
- Python：异常变成未处理的 Future 异常，生产环境静默丢失
- Swift：Task 内未捕获的错误同样静默消失

**做法**：fire-and-forget 的异步调用，入口函数内部必须用 `try/except`（Python）或 `do/catch`（Swift）包裹全部逻辑，至少记日志。

## macOS 代码签名与系统权限的关系

| 签名方式 | Keychain | 辅助功能(TCC) | APNs | Gatekeeper |
|----------|----------|---------------|------|------------|
| 无签名 | ❌ | 每次编译失效 | ❌ | 阻止运行 |
| ad-hoc (`-`) | UserDefaults 降级 | 每次编译失效 | ❌ | 阻止运行 |
| 自签名证书 | Login Keychain ✅ | 跨版本保留 ✅ | ❌ | 需手动信任 |
| Apple Developer | Data Protection Keychain ✅ | 跨版本保留 ✅ | ✅ | 自动信任 |

关键：`kSecUseDataProtectionKeychain` 需要 `keychain-access-groups` entitlement，该 entitlement 仅 Apple Developer 证书 + Provisioning Profile 支持。自签名证书嵌入此 entitlement 会导致 SIGKILL。

## APNs 静默推送——服务端集成要点

- 认证方式选 Auth Key (.p8) 而非证书——一个 Key 可用于所有 App，不过期（证书每年要续）
- JWT 缓存 45 分钟内复用，收到 403 时立即清除缓存强制重新生成
- 收到 410 (Unregistered) 时清除数据库中的 device token，防止持续推送到失效设备
- 推送频率加冷却（如 60 秒/用户），防止断连重连抖动期间刷推送
- 推送是兜底不是主力——WebSocket 重连机制才是第一道防线

## 配置覆盖必须成对——model 和 base_url 不可拆分

如果需要在运行时覆盖 AI 模型，**必须同时覆盖 model 和 base_url**，不能只换其中一个。原因：不同供应商的 API 地址不通用，模型名和 API 地址是绑定关系。

正确做法：模型选择统一走集中绑定系统，每个 slot 包含完整的 `{model, base_url, api_key}` 三元组，业务代码不做部分覆盖。

反模式：系统设置只替换 `slot["model"]` 但保留原 `slot["base_url"]` → 模型名和 API 地址张冠李戴 → 404。

## AI Agent 工具执行分工——客户端触发 vs 服务端执行

Agent 的工具按执行位置分两类：

| 类别 | 执行方 | 典型工具 | 说明 |
|------|--------|----------|------|
| 端侧触发类 | 客户端（桌面/移动） | 发送消息、操作 UI | 模型传最小参数，客户端从 tool_call 事件取参执行 |
| 查询/计算类 | 服务端 | 搜索、预览、数据查询 | 服务端执行后通过 tool_result 返回结果给模型 |

关键约束：
- 端侧触发类工具：模型只传引用标识，客户端在执行前的最后一刻通过轻量 API 获取完整的即时值（如签名 URL）
- 服务端对端侧触发类工具也会执行（用于数据库记账），但实际触发路径只走客户端的 tool_call 事件
- 禁止双轨兜底：一条路走到底

## async 函数 + 同步驱动 = 事件循环阻塞

在 async 框架（如 FastAPI）上使用同步数据库驱动（如 pymysql）和同步 Redis 客户端（如 redis-py）时，每次 `db.query()` / `r.exists()` 都会阻塞事件循环线程，导致同进程内所有 async 任务（心跳、WebSocket 收发、定时器）被饿死。

**症状**：WebSocket 心跳间隔本应 10 秒，实际出现 30-80 秒空档；定时任务频繁 missed。

**修复模式**：用 `asyncio.to_thread()` 将同步 IO 卸载到线程池，事件循环线程只做 async 操作。

```python
def _sync_db_work():
    db = SessionLocal()
    try:
        result = db.query(Model).filter(...).first()
        db.commit()
        return result
    finally:
        db.close()

result = await asyncio.to_thread(_sync_db_work)
```

**不要**为此切换到 async 驱动（aiomysql / aioredis）——改动面太大，且 `asyncio.to_thread` 已够用。

## asyncio.to_thread 中的共享状态约束

`asyncio.to_thread` 将函数移到线程池执行，事件循环线程继续运行其他协程。如果线程池函数直接读写事件循环线程的共享 dict/set（如连接表），会产生竞态。

**规则**：
- 共享 dict/set 的读写只在事件循环线程进行
- 进入 `asyncio.to_thread` 前，将需要的数据提取为局部变量或不可变快照
- 线程函数返回结果后，在事件循环线程更新共享状态

```python
# 正确：进入线程前快照
online_snapshot = frozenset(self._connections.keys())
def _sync_work():
    for uid in online_snapshot:  # 读快照，不读 self._connections
        ...
await asyncio.to_thread(_sync_work)

# 错误：线程中直接读共享 dict
def _sync_work():
    for uid in self._connections:  # 竞态！
        ...
```

## asyncio.Event 不是线程安全的

`asyncio.Event.set()` 只能在创建它的事件循环线程中调用。如果从 `asyncio.to_thread` 的工作线程调用，会产生未定义行为。

**做法**：注册 Event 时保存事件循环引用，跨线程调用时用 `call_soon_threadsafe`。

```python
# 注册
event = asyncio.Event()
loop = asyncio.get_running_loop()
_events[key] = (event, loop)

# 触发（可能从任意线程调用）
entry = _events.get(key)
if entry:
    event, loop = entry
    try:
        asyncio.get_running_loop()
        event.set()  # 同线程，安全
    except RuntimeError:
        loop.call_soon_threadsafe(event.set)  # 跨线程，安全
```

## 大模型工具参数不宜携带长/易失值

当工具需要 URL、token 等长字符串或有时效性的值时，不要让模型在调用参数中传递。

**问题**：
- 签名 URL（如 OSS）通常 400+ 字节，模型容易截断或混淆（预览多张图片后搞不清哪个 URL 对应哪张）
- 签名 URL 有过期时间，模型获取到发送之间可能已过期
- 长参数浪费 token，增加出错概率

**模式**：模型只传引用标识（如 `content_type` + `content_id`），客户端在执行前的最后一刻通过轻量 API 获取完整的即时值。API 复用后端已有的查找逻辑，确保返回的值是新鲜的。

**适用场景**：任何工具参数中包含 URL、文件路径、临时 token 等长/易失值的情况。

## 消息链路断裂——全链路排查模式

当消息发了但下游没有响应时，按数据流经的每一层逐层排查，每层有不同的症状指纹：

| 层 | 检查点 | 正常指纹 | 异常指纹 |
|---|---|---|---|
| 1 | 外部推送到达 | 收到事件通知日志 | 完全无日志 → 回调配置问题 |
| 2 | 调度器拉取 | 同步完成，新增 N 条 | 任务被跳过 → 同步任务卡死 |
| 3 | 内部转发 | 转发成功 200 OK | 无转发日志 → 转发被熔断或未启用 |
| 4 | 处理服务收到 | 处理新消息日志 | 无日志 → 消息没到这一层 |
| 5 | AI Agent 处理 | Agent 启动 → 完成 | 连续 N 轮工具失败，静默终止 → 工具执行异常 |
| 6 | 端侧执行 | 操作完成日志 | 无连接，跳过推送 → 客户端未连接 |

**关键经验**：不要假设问题在某一层——从第 1 层到最后一层逐层过，第一个断裂点就是根因所在。

## 容器重启期间的连锁故障

代码部署触发容器重启时，会在 10-30 秒窗口内引发连锁问题：

1. **502 网关错误** → 客户端 Token 刷新失败 → 连续多次后凭证作废 → 被迫重新登录
2. **工具连续失败** → Agent 的查询工具在容器刚启动时可能全部失败 → 连续失败次数超过阈值触发熔断 → 用户消息被静默丢弃
3. **WebSocket 断开** → 实时推送被跳过 → 客户端收不到事件
4. **定时任务卡死** → `max_instances=1` 的同步任务如果在重启前卡住，会阻塞后续所有同步长达数小时

**教训**：部署窗口期的故障不是"临时的"——会导致真实的用户消息丢失。排查时需关注部署时间窗口与用户投诉的时间重合。

## AI 读图一律传 URL，禁止传 base64

向 AI 模型传图片时，必须传签名 URL（或任何公网可访问的 URL），禁止传 base64 data URL。

| 方式 | Token 消耗 | 延迟 | 带宽 |
|------|-----------|------|------|
| base64 data URL | 原始大小的 ~1.37 倍全部计为输入 token | 慢（大 payload 传给模型 API） | 高 |
| 签名 URL | 仅图片理解的视觉 token（通常几百） | 快（模型自行拉取） | 低 |

**实现模式**：需要让 AI 看图时，先把图片上传对象存储 → 生成签名 URL → 放进 `{"type": "image_url", "image_url": {"url": signed_url}}`。不做 base64 降级——对象存储是基础设施，不可用时直接报错。

## 排查"模型调用 404"的检查清单

当 AI 模型调用返回 404 时，按以下顺序排查：
1. **base_url 与 model 是否匹配**——最常见：供应商 A 的地址 + 供应商 B 的模型名
2. **是否有多层覆盖**——注册中心绑定 → 系统设置 → 代码默认值，任一层的覆盖都可能制造错配
3. **缓存是否过期**——模型配置缓存（如 300 秒 TTL），刚改完绑定可能还在用旧缓存
4. **API Key 是否对应**——每个供应商的 Key 不通用
