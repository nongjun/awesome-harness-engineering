---
description: "Go 编码风格，扩展通用规则"
globs: ["**/*.go", "**/go.mod", "**/go.sum"]
alwaysApply: false
---
# Go 编码风格

> 本文件以 Go 特定内容扩展通用编码风格规则。

## 格式化

- **gofmt** 和 **goimports** 是强制性的——没有风格争论

## 设计原则

- 接受接口，返回结构体
- 保持接口小巧（1-3 个方法）

## 错误处理

向上返回错误时附带业务语义（说明在做什么失败），并保留原始错误供链式判断与日志；使用语言惯用的错误包装方式，避免只返回裸错误或丢失 cause。

## 参考

参见技能：`Go开发模式` 了解全面的 Go 惯用写法和模式。
